import { Lorem } from '../Lorem/Lorem';
import { Section } from '../Section/Section';

export const Contacts = () => {
  return (
    <Section id="contacts">
      <h2>Contacts</h2>
      <Lorem />
    </Section>
  );
};
